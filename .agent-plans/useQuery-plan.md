# Agent Plan: Refactor useQuery from convex/react → @convex-dev/react-query

**Created:** 2026-03-05  
**Status:** ✅ Complete  
**Last Updated:** 2026-03-05

## High-Level Goal

Modernize `useQuery` usage across the codebase to follow idiomatic TanStack Query v5 patterns, removing manual state duplication and centralizing data fetching logic.

## Success Criteria

- [x] All `useQuery` hooks updated to use `useConvexQuery` wrapper
- [x] Manual state variables for loading/error removed where redundant
- [x] Query keys standardized across components
- [x] No manual cache invalidation needed (Convex WebSocket sync handles it)
- [x] All UI components use `query.isLoading`, `query.isError`, `query.data` directly
- [x] `bun run build-ci` passes
- [x] Changes committed to git

## Implementation Plan

### Step 1: Identify All useQuery Usage

**Goal:** Create comprehensive list of all `useQuery` hooks in codebase and their current state

**Reasoning:** Foundation step—need to understand scope before refactoring

**Substeps:**
1. [x] Grep codebase for `useQuery(api.*)` patterns
2. [x] Document each file, hook name, and API endpoint
3. [x] Note current manual state management patterns
4. [x] Categorize by complexity (simple queries vs. dependent queries)

**Success Indicator:**
- [x] Complete list of 20+ useQuery hooks documented
- [x] Dependencies and state patterns understood

**Commit:** Already completed in prior work

**Progress:** ✅ Complete

---

### Step 2: Refactor Simple Queries

**Goal:** Update straightforward `useQuery` calls to use `useConvexQuery` wrapper with proper error handling

**Reasoning:** Simple queries have no dependencies; safe to refactor first

**Substeps:**
1. [x] Update import statements (add `useConvexQuery` from `@convex-dev/react-query`)
2. [x] Wrap query functions: `useConvexQuery(api.endpoint)`
3. [x] Remove redundant manual state variables
4. [x] Update UI to use `query.isLoading`, `query.data`, `query.error`
5. [x] Test each file after refactoring

**Success Indicator:**
- [x] All simple queries refactored
- [x] No TypeScript errors
- [x] No manual loading/error state variables
- [x] UI properly reflects query states

**Commit:** `git commit -m 'refactor: modernize useQuery to TanStack Query v5 patterns'`

**Progress:** ✅ Complete

---

### Step 3: Refactor Dependent Queries

**Goal:** Update queries that depend on other queries or state using `enabled` option

**Reasoning:** Dependent queries need careful handling to avoid unnecessary requests

**Substeps:**
1. [x] Identify queries with conditional dependencies
2. [x] Use `enabled` option: `useConvexQuery(api.fn, { enabled: !!parentData })`
3. [x] Ensure parent queries complete before dependent queries run
4. [x] Test dependency chains

**Success Indicator:**
- [x] Dependent queries only run when appropriate
- [x] No undefined data passed to dependent queries
- [x] Query chain works correctly

**Commit:** Part of step 2 refactoring

**Progress:** ✅ Complete

---

### Step 4: Update Error Handling

**Goal:** Centralize error handling using query error states instead of manual try/catch

**Reasoning:** TanStack Query provides `isError` and `error` states automatically

**Substeps:**
1. [x] Replace manual error states with `query.isError` and `query.error`
2. [x] Display error messages in UI when `query.isError` is true
3. [x] Optionally show retry button using `query.refetch()`
4. [x] Test error scenarios

**Success Indicator:**
- [x] All queries have proper error UI
- [x] Users can retry failed queries
- [x] Error messages are user-friendly

**Commit:** Part of overall refactoring

**Progress:** ✅ Complete

---

## Notes & Decisions

- **No manual cache invalidation needed**: Convex syncs data via WebSockets, so query cache updates automatically when server data changes
- **Query keys**: Automatically generated by `useConvexQuery` based on the API function and arguments
- **Loading states**: `query.isLoading` replaces manual `loading` state variables
- **Completed files** (all refactored in this cycle):
  - `src/0-routes/_authenticated/lre.tsx` — 4 queries refactored
  - `src/0-routes/_authenticated/goals.tsx` — 2 queries refactored
  - `src/0-routes/_authenticated/roster.tsx` — 1 query refactored
  - `src/0-routes/_authenticated/teams.tsx` — 1 query refactored
  - `src/1-components/goals/AddGoalDialog.tsx` — 1 query refactored
  - `src/1-components/goals/EditGoalDialog.tsx` — 1 query refactored
  - And multiple other component files

## References

- [TanStack Query v5 Documentation](https://tanstack.com/query/v5/docs)
- [Convex TanStack Query Integration](https://docs.convex.dev/client/tanstack/tanstack-query/)
- [useConvexQuery API Reference](https://docs.convex.dev/client/tanstack/tanstack-query/)